(executable
  (name      test_yaks_core)
  (modules   test_yaks_core test_selector)
  (libraries yaks_core alcotest)
  (preprocess
    (pps ppx_monadic)))

(alias
 (name runtest)
  (deps test_yaks_core.exe)
  (action (run ${exe:test_yaks_core.exe} -q --color=always)))
